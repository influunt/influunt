'use strict';

describe('Controller: RelatoriosFalhasCtrl', function () {

  var RelatoriosFalhasCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    RelatoriosFalhasCtrl = $controller('RelatoriosFalhasCtrl', {
      $scope: scope
      // place here mocked dependencies
    });
  }));

});
